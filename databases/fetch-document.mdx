---
title: 'Fetch a Document'
description: 'Retrieve the contents of a single document using the Document ID.'
---

You can get a Document using the Document Id:

* The Document Id is a unique identifier for a document in a collection.
* The Document Id is automatically generated when a document is created.
* OR, You can also manually define the Document Id while creating a document.

```shell
curl --location 'https://api.worqhat.com/api/collections/data/fetch/all' \
--header 'Content-Type: application/json' \
--header 'Authorization: Bearer <token>' \
--data '{
    "collection": "Sample collection",
    "documentId": "Sample document id"
}'
```

Try it out in the [API Reference](/api-reference/databases/fetch-document)

<Card title="View API Reference to Implement" icon="link" iconType="duotone" color="#ea5a0c"
      href="/api-reference/databases/fetch-document">
  Visit the API Reference to learn how to Fetch A single documents in your projects. Get
  access to
  Sample Code, API Endpoints and run it right within the browser to test it out.
</Card>
